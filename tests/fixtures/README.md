# Test Fixtures

This directory contains canonical datasets used by automated
verification tools.

These fixtures are intentionally deterministic.

They should only be updated when an ADR explicitly changes.

Do not replace them with runtime logs.

